# Test environment configuration
# This file overrides settings for unit tests

# Override paths to use temporary directories
APP_DATA_DIR=/tmp/violentutf_test/app_data
CONFIG_DIR=/tmp/violentutf_test/config
PYRIT_MEMORY_DB_PATH=/tmp/violentutf_test/app_data/violentutf

# Test-specific settings
ENVIRONMENT=test
DEBUG=True

# JWT and Security
JWT_SECRET_KEY=test-secret-key-for-unit-tests
SECRET_KEY=test-secret-key-for-unit-tests
APISIX_GATEWAY_SECRET=test-gateway-secret

# API Keys
VIOLENTUTF_API_KEY=test-api-key-12345
APISIX_ADMIN_KEY=test-admin-key

# External Services (mocked for tests)
KEYCLOAK_URL=http://mock-keycloak:8080
KEYCLOAK_REALM=violentutf
KEYCLOAK_CLIENT_ID=violentutf-client
KEYCLOAK_CLIENT_SECRET=test-client-secret

# Database
DATABASE_URL=duckdb:///:memory:

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8501"]

# PyRIT Configuration
PYRIT_DB_SALT=test-salt-value